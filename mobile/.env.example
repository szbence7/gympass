# Mobile App Environment Variables
# 
# For local development on real devices, set EXPO_PUBLIC_API_URL to your computer's LAN IP:
#   EXPO_PUBLIC_API_URL=http://192.168.1.100:4000
# 
# For simulators/emulators, localhost works automatically (no env needed).
#
# For production/staging builds:
#   EXPO_PUBLIC_API_URL=https://gymgo.hu
#   (or https://gymgo.hu/api if backend is on /api path)

# API Base URL
# - If not set: Auto-detects from Expo dev server or uses localhost:4000
# - For real devices: Must be set to your computer's LAN IP
# - For production: Set to your production API URL
EXPO_PUBLIC_API_URL=https://gymgo.hu
