# Staff-web Environment Variables
# 
# For local development, these are OPTIONAL - defaults work fine.
# For production builds, set these if needed.

# API Base URL
# - If not set: Uses relative '/api' in production (assumes reverse proxy)
# - If not set in dev: Auto-detects from subdomain or uses localhost:4000
# VITE_API_BASE_URL=https://gymgo.hu/api

# Tenant Base Domain
# - If not set: Defaults to 'gym.local' for dev
# - Production: Set to your tenant domain (e.g. 'gymgo.hu')
# VITE_TENANT_BASE_DOMAIN=gymgo.hu

# API Protocol (for building URLs)
# - If not set: Defaults to 'http' for dev, 'https' for prod
# VITE_API_PROTOCOL=https

# API Port (for building URLs in dev)
# - If not set: Defaults to '4000'
# VITE_API_PORT=4000
