# ================================================================
# GymPass Staff-Web - Environment Variables (Vite)
# ================================================================
# These are OPTIONAL in development (safe defaults provided).
# Vite reads env vars starting with VITE_ prefix only.
# 
# Location: Place this as `.env` or `.env.production` in staff-web/ root
# (NOT in staff-web/src/)

# ----------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------
# API Base URL - where the backend API is hosted
# 
# Dev default: Uses Vite dev proxy (/api -> localhost:4000)
# Prod options:
#   - Leave empty to use relative /api paths (recommended if behind reverse proxy)
#   - Or set explicit URL: VITE_API_BASE_URL=https://api.gympass.hu
# 
# Examples:
# VITE_API_BASE_URL=https://api.gympass.hu
# VITE_API_BASE_URL=https://gympass.hu  (if API is on same domain)
# VITE_API_BASE_URL=   (empty = use relative /api paths)
VITE_API_BASE_URL=

# Admin API Base URL (optional)
# If not set, automatically derived from VITE_API_BASE_URL + /admin path
# VITE_ADMIN_API_BASE_URL=https://api.gympass.hu/api/admin
VITE_ADMIN_API_BASE_URL=

# ----------------------------------------------------------------
# Tenant Configuration
# ----------------------------------------------------------------
# Tenant base domain - for subdomain detection and URL building
# Dev default: gym.local
# Prod: your actual domain
# Example: VITE_TENANT_BASE_DOMAIN=gympass.hu
VITE_TENANT_BASE_DOMAIN=

# ----------------------------------------------------------------
# Advanced (rarely needed)
# ----------------------------------------------------------------
# API protocol (http or https)
# Auto-detected in prod builds, only set if needed
# VITE_API_PROTOCOL=https

# API port (leave empty for standard 80/443)
# Dev default: 4000
# Prod: empty (uses standard HTTPS port 443)
# VITE_API_PORT=

# ----------------------------------------------------------------
# Usage Notes
# ----------------------------------------------------------------
# Development:
#   - No env vars needed!
#   - Vite dev server proxies /api to localhost:4000
#   - Just run: npm run dev
#
# Production Build:
#   1. Create .env.production with values above
#   2. Run: npm run build
#   3. Deploy 'dist' folder
#
# Behind Reverse Proxy (recommended):
#   - Leave VITE_API_BASE_URL empty
#   - Configure proxy to route /api/* to backend
#   - This way staff-web and backend share same origin (no CORS issues)
