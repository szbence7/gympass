# ================================================================
# GymGo SaaS - Staging Environment Variables
# ================================================================
# Copy this file to /srv/gymgo/staging.env and fill in actual values
# DO NOT commit staging.env or prod.env to git (they contain secrets)

# Docker Compose Project Name (used for network/volume isolation)
COMPOSE_PROJECT_NAME=gymgo-staging

# ================================================================
# Database Configuration (Aapanel PostgreSQL)
# ================================================================
# Aapanel PostgreSQL connection from Docker container
# Use host.docker.internal to access host PostgreSQL from Docker
# Format: postgresql://username:password@host.docker.internal:5432/database_name
DATABASE_URL=postgresql://gympass:CHANGE_ME_STAGING_DB_PASSWORD@host.docker.internal:5432/gymgo_staging

# ================================================================
# Server Configuration
# ================================================================
PORT=4000
NODE_ENV=production

# ================================================================
# JWT Secret (MUST BE UNIQUE PER ENVIRONMENT)
# ================================================================
JWT_SECRET=CHANGE_ME_STAGING_JWT_SECRET_MIN_32_CHARS

# ================================================================
# Public URLs (Staging Domain)
# ================================================================
PUBLIC_BASE_URL=https://staging.gymgo.hu
TENANT_BASE_DOMAIN=gymgo.hu
TENANT_PROTOCOL=https
TENANT_PORT=

# ================================================================
# API Configuration
# ================================================================
API_BASE_URL=https://staging.gymgo.hu

# ================================================================
# Stripe Configuration (USE TEST KEYS FOR STAGING)
# ================================================================
# Get from Stripe Dashboard → Developers → API keys
# Use TEST keys (sk_test_...) for staging
STRIPE_SECRET_KEY=sk_test_CHANGE_ME
STRIPE_WEBHOOK_SECRET=whsec_CHANGE_ME
STRIPE_PRICE_ID=price_CHANGE_ME

# ================================================================
# CORS Configuration
# ================================================================
CORS_ALLOWED_ORIGINS=https://staging.gymgo.hu,https://*.gymgo.hu

# ================================================================
# Apple Wallet (Optional)
# ================================================================
# WALLET_TEAM_ID=YOUR_TEAM_ID
# WALLET_PASS_TYPE_ID=pass.com.yourdomain.gympass
# WALLET_ORG_NAME=Gym Name
# WALLET_CERT_P12_PATH=./certs/signerCert.p12
# WALLET_CERT_P12_PASSWORD=
# WALLET_WWDR_CERT_PATH=./certs/wwdr.pem
# WALLET_DEV_UNSIGNED=false

# ================================================================
# Feature Flags
# ================================================================
APPLE_WALLET=false

